export const metadata = { title: "AI Orchestrator | Hermina CRM", description: "Hermina CRM AI Orchestrator" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
